-- ==================== AUTO BANK MODULE ====================
-- Chức năng: Tự động bỏ vũ khí và pet có giá trị vào Bank
-- Requirements: WindUI, global variables from main script
-- ===========================================================

local Module = {}

-- Tắt toàn bộ log console trong module này
local warn = function(...) end

-- ✅ Fetch required globals from the main script
local WindUI = _G.WindUI
local Window = _G.Window
local Players = game:GetService("Players")
local c = _G.PlayerLocal
local d = _G.UserId
local e = _G.HttpService
local ConfigSystem = _G.ConfigSystem
local Toggles = _G.Toggles
local Options = _G.Options
local bQ = _G.GearPerks  -- Perk data
local bR = _G.ItemsModule  -- Items data

-- ✅ Fetch ReplicatedStorage references
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local Shared = ReplicatedStorage:WaitForChild("Shared")
local Bank = Shared:WaitForChild("Bank")
local TransferToBank = Bank:WaitForChild("TransferToBank")
local WithdrawFromBank = Bank:WaitForChild("WithdrawFromBank")

-- ✅ Fetch Player references
local PlayerGui = c:WaitForChild("PlayerGui")
local Profile = PlayerGui:WaitForChild("Profile")
local InventoryItems = Profile:WaitForChild("Inventory"):WaitForChild("Items")
local BankGui = PlayerGui:WaitForChild("Bank")
local BankItems = BankGui:WaitForChild("Bank"):WaitForChild("BankItems")
local BankCounter = BankItems:WaitForChild("Counter")

-- ==================== GLOBALS ====================
local BankGlobals = {
    -- Auto Bank toggles
    isAutoBankingWeapons = false,
    isAutoBankingPets = false,
    
    -- Config cho weapons
    weaponMinTier = 5, -- Tier tối thiểu để bank (mặc định T5)
    weaponPerkThresholds = {}, -- Ngưỡng perk cho weapons
    
    -- Config cho pets
    petMinTier = 5, -- Tier tối thiểu để bank (mặc định T5)
    petPerkThresholds = {}, -- Ngưỡng perk cho pets
    
    -- Statistics
    totalWeaponsBanked = 0,
    totalPetsBanked = 0,
    sessionStartTime = 0,
    
    -- Config file
    ConfigFile = "AnyaHub_BankConfig_" .. c.Name .. ".txt"
}

-- ==================== UTILITY FUNCTIONS ====================
local function libNoti(msg, duration)
    WindUI:Notify({
        Title = "Auto Bank",
        Content = msg,
        Duration = duration or 5
    })
end

local function formatNumber(num)
    if num >= 1000000 then
        return string.format("%.1fM", num / 1000000)
    elseif num >= 1000 then
        return string.format("%.1fK", num / 1000)
    else
        return tostring(num)
    end
end

local function timeElapsed(seconds)
    local hours = math.floor(seconds / 3600)
    local mins = math.floor((seconds % 3600) / 60)
    local secs = seconds % 60
    
    if hours > 0 then
        return string.format("%dh %dm %ds", hours, mins, secs)
    elseif mins > 0 then
        return string.format("%dm %ds", mins, secs)
    else
        return string.format("%ds", secs)
    end
end

-- ==================== BANK FUNCTIONS ====================
-- Lấy thông tin bank capacity (18/50)
local function getBankInfo()
    if not BankCounter or not BankCounter.Value then
        return 0, 0
    end
    
    local counterText = BankCounter.Value
    local current, max = string.match(counterText, "(%d+)/(%d+)")
    
    return tonumber(current) or 0, tonumber(max) or 0
end

-- Kiểm tra xem bank có đầy không
local function isBankFull()
    local current, max = getBankInfo()
    return current >= max
end

-- Bỏ item vào bank
local function transferToBank(item, quantity)
    if not item or not item.Parent then
        return false
    end
    
    if isBankFull() then
        libNoti("⚠️ Bank đã đầy! Không thể thêm item.", 3)
        return false
    end
    
    local success = pcall(function()
        TransferToBank:FireServer(item, quantity or 1)
    end)
    
    if success then
        task.wait(0.2) -- Wait for replication
    end
    
    return success
end

-- Lấy item từ bank
local function withdrawFromBank(item, quantity)
    if not item or not item.Parent then
        return false
    end
    
    local success = pcall(function()
        WithdrawFromBank:FireServer(item, quantity or 1)
    end)
    
    if success then
        task.wait(0.2) -- Wait for replication
    end
    
    return success
end

-- ==================== WEAPON FUNCTIONS ====================
-- Lấy tier của weapon
local function getWeaponTier(weaponItem)
    if not weaponItem or not weaponItem:FindFirstChild("Tier") then
        return nil
    end
    
    return weaponItem.Tier.Value
end

-- Fetch weapon perk list
local function getWeaponPerksList()
    local weaponPerks = {}
    
    for perkKey, perkData in pairs(bQ) do
        -- ✅ Chỉ lấy perks có thể xuất hiện trên vũ khí
        local canAppearOnWeapon = not perkData.ArmorOnly and not perkData.PetOnly
        
        if canAppearOnWeapon and not perkData.Disabled then
            local statRange = perkData.StatRange
            
            if statRange then
                table.insert(weaponPerks, {
                    key = perkKey,
                    name = perkData.DisplayName,
                    desc = perkData.DisplayDesc,
                    min = statRange[1] * 100,
                    max = statRange[2] * 100
                })
            end
        end
    end
    
    table.sort(weaponPerks, function(a, b) return a.name < b.name end)
    return weaponPerks
end

-- Kiểm tra weapon có đáp ứng điều kiện bank không
local function shouldBankWeapon(weaponItem)
    local itemData = bR[weaponItem.Name]
    if not itemData or (itemData.Type ~= "Weapon" and itemData.Type ~= "Armor") then
        return false, "Not a weapon/armor"
    end
    
    -- Kiểm tra tier
    local tier = getWeaponTier(weaponItem)
    if not tier or tier < BankGlobals.weaponMinTier then
        return false, string.format("Tier %d < min tier %d", tier or 0, BankGlobals.weaponMinTier)
    end
    
    -- Kiểm tra perks
    local hasValidPerk = false
    local bestPerkInfo = nil
    local shouldKeep = false
    
    for _, child in ipairs(weaponItem:GetChildren()) do
        if child:IsA("StringValue") and child:FindFirstChild("PerkValue") then
            local perkName = child.Value
            local perkValue = child.PerkValue.Value
            
            local perkData = bQ[perkName]
            
            if perkData then
                local canAppearOnWeapon = not perkData.ArmorOnly and not perkData.PetOnly
                local statRange = perkData.StatRange
                
                if canAppearOnWeapon and statRange then
                    hasValidPerk = true
                    
                    local threshold = BankGlobals.weaponPerkThresholds[perkName]
                    if not threshold then
                        threshold = 999
                    else
                        threshold = tonumber(threshold) or 999
                    end
                    
                    local perkPercent = math.round(perkValue * 100)
                    local meetsThreshold = perkPercent >= threshold
                    
                    if meetsThreshold and not shouldKeep then
                        shouldKeep = true
                    end
                    
                    if not bestPerkInfo or meetsThreshold or (not shouldKeep and perkPercent > bestPerkInfo.value) then
                        bestPerkInfo = {
                            name = perkData.DisplayName,
                            value = perkPercent,
                            threshold = threshold,
                            meets = meetsThreshold
                        }
                    end
                end
            end
        end
    end
    
    if not hasValidPerk then
        return false, "No valid perk"
    end
    
    if not shouldKeep then
        return false, string.format("%s: %.1f%% < %.1f%%", bestPerkInfo.name, bestPerkInfo.value, bestPerkInfo.threshold)
    end
    
    return true, string.format("%s: %.1f%% ≥ %.1f%%", bestPerkInfo.name, bestPerkInfo.value, bestPerkInfo.threshold)
end

-- Lấy tất cả weapons trong inventory
local function getAllWeaponsInInventory()
    local weapons = {}
    
    for _, item in ipairs(InventoryItems:GetChildren()) do
        if item:IsA("Folder") then
            local itemData = bR[item.Name]
            if itemData and (itemData.Type == "Weapon" or itemData.Type == "Armor") then
                table.insert(weapons, item)
            end
        end
    end
    
    return weapons
end

-- Bank một weapon
local function bankWeapon(weaponItem)
    local shouldBank, reason = shouldBankWeapon(weaponItem)
    
    if not shouldBank then
        return false, reason
    end
    
    local success = transferToBank(weaponItem, 1)
    
    if success then
        BankGlobals.totalWeaponsBanked = BankGlobals.totalWeaponsBanked + 1
        local itemData = bR[weaponItem.Name]
        libNoti(string.format("✅ Banked weapon: %s | %s", itemData.DisplayKey or weaponItem.Name, reason), 2)
    end
    
    return success, reason
end

-- ==================== PET FUNCTIONS ====================
-- Lấy tier của pet
local function getPetTier(petItem)
    if not petItem or not petItem:FindFirstChild("Variant") then
        return nil
    end
    
    local petName = petItem.Name
    local variantValue = petItem.Variant.Value
    
    local itemData = bR[petName]
    if not itemData or itemData.Type ~= "Pet" or not itemData.Variants then
        return nil
    end
    
    for _, variant in ipairs(itemData.Variants) do
        if variant[1] == variantValue then
            return variant[4]
        end
    end
    
    return nil
end

-- Fetch pet perk list
local function getPetPerksList()
    local petPerks = {}
    
    for perkKey, perkData in pairs(bQ) do
        local canAppearOnPet = (not perkData.WeaponOnly and not perkData.ArmorOnly) or perkData.PetOnly
        
        if canAppearOnPet and not perkData.Disabled then
            local statRange = perkData.PetStatRange or perkData.StatRange
            
            if statRange then
                table.insert(petPerks, {
                    key = perkKey,
                    name = perkData.DisplayName,
                    desc = perkData.DisplayDesc,
                    min = statRange[1] * 100,
                    max = statRange[2] * 100
                })
            end
        end
    end
    
    table.sort(petPerks, function(a, b) return a.name < b.name end)
    return petPerks
end

-- Kiểm tra pet có đáp ứng điều kiện bank không
local function shouldBankPet(petItem)
    local itemData = bR[petItem.Name]
    if not itemData or itemData.Type ~= "Pet" then
        return false, "Not a pet"
    end
    
    -- Kiểm tra tier
    local tier = getPetTier(petItem)
    if not tier or tier < BankGlobals.petMinTier then
        return false, string.format("Tier %d < min tier %d", tier or 0, BankGlobals.petMinTier)
    end
    
    -- Kiểm tra perks
    local hasValidPerk = false
    local bestPerkInfo = nil
    local shouldKeep = false
    
    for _, child in ipairs(petItem:GetChildren()) do
        if child:IsA("StringValue") and child:FindFirstChild("PerkValue") then
            local perkName = child.Value
            local perkValue = child.PerkValue.Value
            
            local perkData = bQ[perkName]
            
            if perkData then
                local canAppearOnPet = (not perkData.WeaponOnly and not perkData.ArmorOnly) or perkData.PetOnly
                local statRange = perkData.PetStatRange or perkData.StatRange
                
                if canAppearOnPet and statRange then
                    hasValidPerk = true
                    
                    local threshold = BankGlobals.petPerkThresholds[perkName]
                    if not threshold then
                        threshold = 999
                    else
                        threshold = tonumber(threshold) or 999
                    end
                    
                    local perkPercent = math.round(perkValue * 100)
                    local meetsThreshold = perkPercent >= threshold
                    
                    if meetsThreshold and not shouldKeep then
                        shouldKeep = true
                    end
                    
                    if not bestPerkInfo or meetsThreshold or (not shouldKeep and perkPercent > bestPerkInfo.value) then
                        bestPerkInfo = {
                            name = perkData.DisplayName,
                            value = perkPercent,
                            threshold = threshold,
                            meets = meetsThreshold
                        }
                    end
                end
            end
        end
    end
    
    if not hasValidPerk then
        return false, "No valid perk"
    end
    
    if not shouldKeep then
        return false, string.format("%s: %.1f%% < %.1f%%", bestPerkInfo.name, bestPerkInfo.value, bestPerkInfo.threshold)
    end
    
    return true, string.format("%s: %.1f%% ≥ %.1f%%", bestPerkInfo.name, bestPerkInfo.value, bestPerkInfo.threshold)
end

-- Lấy tất cả pets trong inventory
local function getAllPetsInInventory()
    local pets = {}
    
    for _, item in ipairs(InventoryItems:GetChildren()) do
        if item:IsA("Folder") then
            local itemData = bR[item.Name]
            if itemData and itemData.Type == "Pet" then
                table.insert(pets, item)
            end
        end
    end
    
    return pets
end

-- Bank một pet
local function bankPet(petItem)
    local shouldBank, reason = shouldBankPet(petItem)
    
    if not shouldBank then
        return false, reason
    end
    
    local success = transferToBank(petItem, 1)
    
    if success then
        BankGlobals.totalPetsBanked = BankGlobals.totalPetsBanked + 1
        local itemData = bR[petItem.Name]
        libNoti(string.format("✅ Banked pet: %s | %s", itemData.DisplayKey or petItem.Name, reason), 2)
    end
    
    return success, reason
end

-- ==================== AUTO FUNCTIONS ====================
local function startAutoBank()
    if BankGlobals.sessionStartTime == 0 then
        BankGlobals.sessionStartTime = tick()
    end
    
    -- ✅ Auto Bank Weapons Listener
    if BankGlobals.isAutoBankingWeapons then
        InventoryItems.ChildAdded:Connect(function(item)
            task.wait(0.1)
            
            local autoBankWeaponsEnabled = _G.Toggles and _G.Toggles.AutoBankWeapons and _G.Toggles.AutoBankWeapons.Value or false
            
            if not autoBankWeaponsEnabled then
                return
            end
            
            local itemData = bR[item.Name]
            if not itemData or (itemData.Type ~= "Weapon" and itemData.Type ~= "Armor") then
                return
            end
            
            bankWeapon(item)
        end)
    end
    
    -- ✅ Auto Bank Pets Listener
    if BankGlobals.isAutoBankingPets then
        InventoryItems.ChildAdded:Connect(function(item)
            task.wait(0.1)
            
            local autoBankPetsEnabled = _G.Toggles and _G.Toggles.AutoBankPets and _G.Toggles.AutoBankPets.Value or false
            
            if not autoBankPetsEnabled then
                return
            end
            
            local itemData = bR[item.Name]
            if not itemData or itemData.Type ~= "Pet" then
                return
            end
            
            bankPet(item)
        end)
    end
end

-- ==================== CONFIG FUNCTIONS ====================
local function saveBankConfig()
    if not writefile then return end
    
    local data = {
        totalWeaponsBanked = BankGlobals.totalWeaponsBanked,
        totalPetsBanked = BankGlobals.totalPetsBanked
    }
    
    pcall(function()
        writefile(BankGlobals.ConfigFile, e:JSONEncode(data))
    end)
end

local function loadBankConfig()
    if not (isfile and readfile and isfile(BankGlobals.ConfigFile)) then
        return false
    end
    
    local success = pcall(function()
        local data = e:JSONDecode(readfile(BankGlobals.ConfigFile))
        
        BankGlobals.totalWeaponsBanked = data.totalWeaponsBanked or 0
        BankGlobals.totalPetsBanked = data.totalPetsBanked or 0
    end)
    
    return success
end

-- ==================== UI CREATION ====================
local function createInventoryTab()
    local TabInventory = Window:Tab({
        Title = "Inventory",
        Icon = "package",
        Locked = false
    })
    
    TabInventory:Section({ Title = "🏦 Auto Bank System" })
    
    -- Paragraph hiển thị bank info
    local BankInfoParagraph = TabInventory:Paragraph({
        Title = "📊 Bank Status",
        Description = "Loading..."
    })
    
    task.spawn(function()
        while true do
            local current, max = getBankInfo()
            BankInfoParagraph:SetDesc(string.format("📦 Bank: %d/%d items", current, max))
            task.wait(1)
        end
    end)
    
    TabInventory:Divider({ Text = "Auto Bank Weapons" })
    
    -- Auto Bank Weapons Toggle
    local AutoBankWeaponsToggle = TabInventory:Toggle({
        Title = "⚔️ Auto Bank Weapons",
        Description = "Tự động bank weapons khi nhận được",
        Default = false,
        Callback = function(state)
            if _G.Toggles and _G.Toggles.AutoBankWeapons then
                _G.Toggles.AutoBankWeapons:SetValue(state)
            end
            BankGlobals.isAutoBankingWeapons = state
            
            if state then
                libNoti("✅ Auto Bank Weapons đã BẬT", 2)
            else
                libNoti("❌ Auto Bank Weapons đã TẮT", 2)
            end
        end
    })
    ConfigSystem.registerUIElement("AutoBankWeapons", AutoBankWeaponsToggle)
    
    -- Weapon Min Tier Slider
    local WeaponMinTierSlider = TabInventory:Slider({
        Title = "Weapon Min Tier",
        Description = "Tier tối thiểu để bank weapon",
        Step = 1,
        Value = {
            Min = 1,
            Max = 5,
            Default = BankGlobals.weaponMinTier
        },
        Callback = function(value)
            BankGlobals.weaponMinTier = value
        end
    })
    ConfigSystem.registerUIElement("BankWeaponMinTier", WeaponMinTierSlider)
    
    TabInventory:Divider({ Text = "Weapon Perk Thresholds" })
    
    -- ✅ Tạo slider cho từng weapon perk
    local weaponPerks = getWeaponPerksList()
    for _, perk in ipairs(weaponPerks) do
        if not BankGlobals.weaponPerkThresholds[perk.key] then
            BankGlobals.weaponPerkThresholds[perk.key] = math.round(perk.min)
        end
        
        local slider = TabInventory:Slider({
            Title = perk.name,
            Description = perk.desc .. string.format(" (Range: %.1f%% - %.1f%%)", perk.min, perk.max),
            Step = 1,
            Value = {
                Min = math.round(perk.min),
                Max = math.round(perk.max),
                Default = BankGlobals.weaponPerkThresholds[perk.key]
            },
            Callback = function(value)
                BankGlobals.weaponPerkThresholds[perk.key] = value
            end
        })
        ConfigSystem.registerUIElement("BankWeaponPerk_" .. perk.key, slider)
    end
    
    TabInventory:Divider({ Text = "Auto Bank Pets" })
    
    -- Auto Bank Pets Toggle
    local AutoBankPetsToggle = TabInventory:Toggle({
        Title = "🐾 Auto Bank Pets",
        Description = "Tự động bank pets khi nhận được",
        Default = false,
        Callback = function(state)
            if _G.Toggles and _G.Toggles.AutoBankPets then
                _G.Toggles.AutoBankPets:SetValue(state)
            end
            BankGlobals.isAutoBankingPets = state
            
            if state then
                libNoti("✅ Auto Bank Pets đã BẬT", 2)
            else
                libNoti("❌ Auto Bank Pets đã TẮT", 2)
            end
        end
    })
    ConfigSystem.registerUIElement("AutoBankPets", AutoBankPetsToggle)
    
    -- Pet Min Tier Slider
    local PetMinTierSlider = TabInventory:Slider({
        Title = "Pet Min Tier",
        Description = "Tier tối thiểu để bank pet",
        Step = 1,
        Value = {
            Min = 1,
            Max = 5,
            Default = BankGlobals.petMinTier
        },
        Callback = function(value)
            BankGlobals.petMinTier = value
        end
    })
    ConfigSystem.registerUIElement("BankPetMinTier", PetMinTierSlider)
    
    TabInventory:Divider({ Text = "Pet Perk Thresholds" })
    
    -- ✅ Tạo slider cho từng pet perk
    local petPerks = getPetPerksList()
    for _, perk in ipairs(petPerks) do
        if not BankGlobals.petPerkThresholds[perk.key] then
            BankGlobals.petPerkThresholds[perk.key] = math.round(perk.min)
        end
        
        local slider = TabInventory:Slider({
            Title = perk.name,
            Description = perk.desc .. string.format(" (Range: %.1f%% - %.1f%%)", perk.min, perk.max),
            Step = 1,
            Value = {
                Min = math.round(perk.min),
                Max = math.round(perk.max),
                Default = BankGlobals.petPerkThresholds[perk.key]
            },
            Callback = function(value)
                BankGlobals.petPerkThresholds[perk.key] = value
            end
        })
        ConfigSystem.registerUIElement("BankPetPerk_" .. perk.key, slider)
    end
    
    TabInventory:Divider({ Text = "Manual Actions" })
    
    -- Button Bank All Weapons
    TabInventory:Button({
        Title = "⚔️ Bank All Qualifying Weapons",
        Description = "Bank tất cả weapons đủ điều kiện (double-click)",
        Callback = function()
            local weapons = getAllWeaponsInInventory()
            local bankedCount = 0
            
            for _, weapon in ipairs(weapons) do
                local success, reason = bankWeapon(weapon)
                if success then
                    bankedCount = bankedCount + 1
                    task.wait(0.2)
                end
            end
            
            if bankedCount > 0 then
                libNoti(string.format("✅ Đã bank %d weapons!", bankedCount), 3)
            else
                libNoti("⚠️ Không có weapon nào đủ điều kiện để bank", 3)
            end
        end
    })
    
    -- Button Bank All Pets
    TabInventory:Button({
        Title = "🐾 Bank All Qualifying Pets",
        Description = "Bank tất cả pets đủ điều kiện (double-click)",
        Callback = function()
            local pets = getAllPetsInInventory()
            local bankedCount = 0
            
            for _, pet in ipairs(pets) do
                local success, reason = bankPet(pet)
                if success then
                    bankedCount = bankedCount + 1
                    task.wait(0.2)
                end
            end
            
            if bankedCount > 0 then
                libNoti(string.format("✅ Đã bank %d pets!", bankedCount), 3)
            else
                libNoti("⚠️ Không có pet nào đủ điều kiện để bank", 3)
            end
        end
    })
    
    -- Button Reset Statistics
    TabInventory:Button({
        Title = "🔄 Reset Statistics",
        Description = "Reset session statistics",
        Callback = function()
            BankGlobals.totalWeaponsBanked = 0
            BankGlobals.totalPetsBanked = 0
            BankGlobals.sessionStartTime = tick()
            saveBankConfig()
            libNoti("✅ Statistics đã được reset", 2)
        end
    })
    
    TabInventory:Divider({ Text = "Statistics" })
    
    -- Statistics Paragraph
    local StatsParagraph = TabInventory:Paragraph({
        Title = "📊 Session Stats",
        Description = "Loading..."
    })
    
    task.spawn(function()
        while true do
            local elapsed = tick() - BankGlobals.sessionStartTime
            local stats = string.format(
                "⏱️ Time: %s\n" ..
                "⚔️ Weapons Banked: %s\n" ..
                "🐾 Pets Banked: %s",
                timeElapsed(math.floor(elapsed)),
                formatNumber(BankGlobals.totalWeaponsBanked),
                formatNumber(BankGlobals.totalPetsBanked)
            )
            
            StatsParagraph:SetDesc(stats)
            task.wait(1)
        end
    end)
end

-- ==================== INIT MODULE ====================
function Module:Init()
    -- Load config
    loadBankConfig()
    
    -- Create UI
    createInventoryTab()
    
    -- Start auto bank listeners
    startAutoBank()
    
    -- ✅ Hook vào ConfigSystem để save/load BankGlobals
    local originalGetConfigData = ConfigSystem.getConfigData
    ConfigSystem.getConfigData = function()
        local data = originalGetConfigData()
        
        -- Chuyển đổi weapon perk thresholds
        local weaponPerkConfig = {}
        for perkKey, value in pairs(BankGlobals.weaponPerkThresholds) do
            weaponPerkConfig["BankWeaponPerk_" .. perkKey] = value
        end
        
        -- Chuyển đổi pet perk thresholds
        local petPerkConfig = {}
        for perkKey, value in pairs(BankGlobals.petPerkThresholds) do
            petPerkConfig["BankPetPerk_" .. perkKey] = value
        end
        
        data.BankGlobals = {
            weaponMinTier = BankGlobals.weaponMinTier,
            petMinTier = BankGlobals.petMinTier,
            weaponPerkThresholds = weaponPerkConfig,
            petPerkThresholds = petPerkConfig
        }
        
        return data
    end
    
    local originalLoadConfigData = ConfigSystem.loadConfigData
    ConfigSystem.loadConfigData = function(data)
        originalLoadConfigData(data)
        
        if data and data.BankGlobals then
            -- Load min tiers
            if data.BankGlobals.weaponMinTier then
                BankGlobals.weaponMinTier = data.BankGlobals.weaponMinTier
            end
            
            if data.BankGlobals.petMinTier then
                BankGlobals.petMinTier = data.BankGlobals.petMinTier
            end
            
            -- Load weapon perk thresholds
            if data.BankGlobals.weaponPerkThresholds then
                for key, value in pairs(data.BankGlobals.weaponPerkThresholds) do
                    local numValue = tonumber(value) or value
                    local perkKey = key
                    if string.sub(key, 1, 17) == "BankWeaponPerk_" then
                        perkKey = string.sub(key, 18)
                    end
                    BankGlobals.weaponPerkThresholds[perkKey] = numValue
                end
            end
            
            -- Load pet perk thresholds
            if data.BankGlobals.petPerkThresholds then
                for key, value in pairs(data.BankGlobals.petPerkThresholds) do
                    local numValue = tonumber(value) or value
                    local perkKey = key
                    if string.sub(key, 1, 14) == "BankPetPerk_" then
                        perkKey = string.sub(key, 15)
                    end
                    BankGlobals.petPerkThresholds[perkKey] = numValue
                end
            end
            
            -- Update UI sliders
            task.spawn(function()
                task.wait(0.5)
                
                -- Update weapon min tier slider
                local weaponMinTierSlider = ConfigSystem.UIElements["BankWeaponMinTier"]
                if weaponMinTierSlider and weaponMinTierSlider.Set then
                    pcall(function()
                        weaponMinTierSlider:Set(BankGlobals.weaponMinTier)
                    end)
                end
                
                -- Update pet min tier slider
                local petMinTierSlider = ConfigSystem.UIElements["BankPetMinTier"]
                if petMinTierSlider and petMinTierSlider.Set then
                    pcall(function()
                        petMinTierSlider:Set(BankGlobals.petMinTier)
                    end)
                end
                
                -- Update weapon perk sliders
                for perkKey, value in pairs(BankGlobals.weaponPerkThresholds) do
                    local sliderKey = "BankWeaponPerk_" .. perkKey
                    local slider = ConfigSystem.UIElements[sliderKey]
                    if slider and slider.Set then
                        pcall(function()
                            slider:Set(tonumber(value))
                        end)
                    end
                end
                
                -- Update pet perk sliders
                for perkKey, value in pairs(BankGlobals.petPerkThresholds) do
                    local sliderKey = "BankPetPerk_" .. perkKey
                    local slider = ConfigSystem.UIElements[sliderKey]
                    if slider and slider.Set then
                        pcall(function()
                            slider:Set(tonumber(value))
                        end)
                    end
                end
            end)
        end
    end
end

return Module
